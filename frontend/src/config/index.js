// This file is the target of: import defaultConfig from "config";
const defaultConfig = {
  theme: 'default',
  locale: 'en',
  layout: 'vertical',
  // add any keys your ConfigContext merges/reads
};

export default defaultConfig;
