diff --git a/backend/routes/market.py b/backend/routes/market.py
        @@
         from flask import Blueprint, jsonify
         import json

+        @bp.get("/market/latest")
+        def market_latest():
+            row = dl.db.execute(
+                "SELECT payload FROM monitor_ledger "
+                "WHERE monitor='market_monitor' ORDER BY created_at DESC LIMIT 1"
+            ).fetchone()
+            if not row:
+                return jsonify({})
+            payload = json.loads(row["payload"])
+            return jsonify({ d["asset"]: d["windows"] for d in payload["details"] })