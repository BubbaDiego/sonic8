diff --git a/backend/services/price_sync_service.py b/backend/services/price_sync_service.py
        @@
-            if asset in ["BTC","ETH","SOL"]:
-                price = coingecko_price_fetch(asset)
+            if asset == "SPX":
+                import yfinance as yf
+                price = yf.Ticker("^GSPC").history(period="1d").iloc[-1]["Close"]
+            elif asset in ["BTC","ETH","SOL"]:
+                price = coingecko_price_fetch(asset)
             else:
                 continue