diff --git a/backend/core/monitor_core/market_monitor.py b/backend/core/monitor_core/market_monitor.py
index e69de29..b4c8f23 100644
--- a/backend/core/monitor_core/market_monitor.py
+++ b/backend/core/monitor_core/market_monitor.py
@@
 class MarketMonitor(BaseMonitor):
-    name = "market_monitor"
-    ASSETS = ["BTC", "ETH", "SOL"]
+    name    = "market_monitor"
+    ASSETS  = ["SPX", "BTC", "ETH", "SOL"]
+    WINDOWS = {
+        "15m": 15 * 60,
+        "1h" : 60 * 60,
+        "3h" : 3  * 60 * 60,
+        "6h" : 6  * 60 * 60,
+        "12h": 12 * 60 * 60,
+        "24h": 24 * 60 * 60,
+    }

     def _cfg(self):
-        cfg = self.dl.system.get_var(self.name) or {"thresholds": {}, "baseline": {}}
+        cfg = self.dl.system.get_var(self.name) or {}
+        cfg.setdefault("thresholds", {})
+        cfg.setdefault("baseline", {})

-        for asset in self.ASSETS:
-            cfg["thresholds"].setdefault(asset, 5.0)
+        for asset in self.ASSETS:
+            cfg["baseline"].setdefault(asset, {"value":0, "time":0})
+
+            # --- migrate old flat thresholds to 24h window ---
+            cur = cfg["thresholds"].get(asset, 5.0)
+            if isinstance(cur, (int, float, str)):
+                cur = {"24h": float(cur)}
+
+            cfg["thresholds"][asset] = { **{w:2.0 for w in self.WINDOWS}, **cur }

-        return cfg
+        self.dl.system.set_var(self.name, cfg)
+        return cfg
+
+    # ---- helper ----
+    def _price_at(self, asset:str, seconds_ago:int) -> float:
+        ts_cut = datetime.now(timezone.utc).timestamp() - seconds_ago
+        row = self.dl.db.execute(
+            "SELECT current_price FROM prices "
+            "WHERE asset_type=? AND last_update_time<=? "
+            "ORDER BY last_update_time DESC LIMIT 1",
+            (asset, ts_cut)
+        ).fetchone()
+        return float(row["current_price"]) if row else 0.0
+
+    def _do_work(self):
+        cfg = self._cfg()
+        prices = self._latest_prices()
+        results = []
+        triggered_any = False
+
+        for asset in self.ASSETS:
+            cur_price = prices[asset]
+            windows_data = {}
+
+            for win, secs in self.WINDOWS.items():
+                prev_price = self._price_at(asset, secs) or cur_price
+                pct = (cur_price - prev_price) / prev_price * 100.0
+                thr = cfg["thresholds"][asset][win]
+                hit = abs(pct) >= thr
+                windows_data[win] = {"pct_move":round(pct,4),"threshold":thr,"trigger":hit}
+                triggered_any |= hit
+
+            # blast‑radius / baseline code untouched
+            results.append({"asset":asset,"windows":windows_data})
+
+        return {"triggered": triggered_any, "details": results}